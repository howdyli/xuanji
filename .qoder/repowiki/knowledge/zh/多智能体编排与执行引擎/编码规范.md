- 在 YAML 配置文件中统一管理智能体的角色（role）、目标（goal）及背景故事（backstory），并通过代码动态注入会话相关变量。
- 在执行工具调用前实施输入参数标准化，自动将 Python 风格的布尔值/空值转换为 MCP 兼容的 JSON 原生类型。
- 采用 Pydantic 模型（如 `MainTaskOutput`）强制约束智能体的最终输出格式，确保业务逻辑能可靠地提取回复内容与技能使用记录。